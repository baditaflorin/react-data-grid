import { textEditor } from '../../../../src'; // Update the path according to your project structure

const ContactColumn = {
  key: 'contact',
  name: 'Contact',
  renderEditCell: textEditor
};

export default ContactColumn;
